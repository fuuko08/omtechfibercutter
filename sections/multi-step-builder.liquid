<style>
  .center-button {
    display: flex;
    flex-direction: row;
    align-content: center;
    justify-content: center;
}
  button {
    font-size: 21px;
    border: 1px solid;
    width: 200px;
    padding: 10px;
    position: relative;
    bottom: 20px;
}
  .row {
    display: flex;
    justify-content: space-around;
}
  /* Slideshow container */
.slideshow-container {
  max-width: 1000px;
  position: relative;
  margin: auto;
}

/* Next & previous buttons */
.prev, .next {
  cursor: pointer;
  position: absolute;
  top: 50%;
  width: auto;
  padding: 16px;
  margin-top: -22px;
  color: white;
  font-weight: bold;
  font-size: 18px;
  transition: 0.6s ease;
  border-radius: 0 3px 3px 0;
  user-select: none;
}

/* Position the "next button" to the right */
.next {
  right: 0;
  border-radius: 3px 0 0 3px;
}

/* On hover, add a black background color with a little bit see-through */
.prev:hover, .next:hover {
  background-color: rgba(0,0,0,0.8);
}

/* Caption text */
.text {
  color: #f2f2f2;
  font-size: 15px;
  padding: 8px 12px;
  position: absolute;
  bottom: 8px;
  width: 100%;
  text-align: center;
}

/* Number text (1/3 etc) */
.numbertext {
  color: #f2f2f2;
  font-size: 12px;
  padding: 8px 12px;
  position: absolute;
  top: 0;
}

/* The dots/bullets/indicators */
.dot {
  cursor: pointer;
  height: 15px;
  width: 15px;
  margin: 0 2px;
  background-color: #bbb;
  border-radius: 50%;
  display: inline-block;
  transition: background-color 0.6s ease;
}

.active, .dot:hover {
  background-color: #717171;
}

/* Fading animation */
.fade {
  animation-name: fade;
  animation-duration: 1.5s;
}

@keyframes fade {
  from {opacity: .4} 
  to {opacity: 1}
}

/* On smaller screens, decrease text size */
@media only screen and (max-width: 300px) {
  .prev, .next,.text {font-size: 11px}
}
</style>
{%- if section.settings.divider -%}<div class="section--divider">{%- endif -%}

<div class="page-width">
  {%- if section.settings.title != blank -%}
    <div class="section-header text-{{ section.settings.heading_position }}">
      <h2 class="section-header__title {{ section.settings.heading_size }}">{{ section.settings.title | escape }}</h2>
    </div>
  {%- endif -%}
 <!DOCTYPE html>
<html>
<head>
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
* {box-sizing: border-box}
body {font-family: Verdana, sans-serif; margin:0}
.mySlides {display: none}
img {vertical-align: middle;}

/* Slideshow container */
.slideshow-container {
  max-width: 1000px;
  position: relative;
  margin: auto;
}

/* Next & previous buttons */
.prev, .next {
  cursor: pointer;
  position: absolute;
  top: 50%;
  width: auto;
  padding: 16px;
  margin-top: -22px;
  color: white;
  font-weight: bold;
  font-size: 18px;
  transition: 0.6s ease;
  border-radius: 0 3px 3px 0;
  user-select: none;
}

/* Position the "next button" to the right */
.next {
  right: 0;
  border-radius: 3px 0 0 3px;
}

/* On hover, add a black background color with a little bit see-through */
.prev:hover, .next:hover {
  background-color: rgba(0,0,0,0.8);
}

/* Caption text */
.text {
  color: #f2f2f2;
  font-size: 15px;
  padding: 8px 12px;
  position: absolute;
  bottom: 8px;
  width: 100%;
  text-align: center;
}

/* Number text (1/3 etc) */
.numbertext {
  color: #f2f2f2;
  font-size: 12px;
  padding: 8px 12px;
  position: absolute;
  top: 0;
}

/* The dots/bullets/indicators */
.dot {
  cursor: pointer;
  height: 15px;
  width: 15px;
  margin: 0 2px;
  background-color: #bbb;
  border-radius: 50%;
  display: inline-block;
  transition: background-color 0.6s ease;
}

.active, .dot:hover {
  background-color: #717171;
}

/* Fading animation */
.fade {
  animation-name: fade;
  animation-duration: 1.5s;
}

@keyframes fade {
  from {opacity: .4} 
  to {opacity: 1}
}

/* On smaller screens, decrease text size */
@media only screen and (max-width: 300px) {
  .prev, .next,.text {font-size: 11px}
}
</style>
</head>
<body>

<div class="slideshow-container">
<div class="mySlides fade">
  <div class="numbertext">1 / 3</div>
  <div class="center-button">
  <button class="button-first">
  {% if section.settings.button_label1 != blank and section.settings.button_link1 != blank %}
    <a href="{{ section.settings.button_link1 }}">{{ section.settings.button_label1 }}</a>
  {% endif %}
</button>
   <button class ="button-second">
  {% if section.settings.button_label2 != blank and section.settings.button_link2 != blank %}
    <a href="{{ section.settings.button_link2 }}">{{ section.settings.button_label2 }}</a>
  {% endif %}
</button>
  </div>
  {%- if section.blocks.size > 0 -%}
    <div class="new-products-sec swiper index_proSwiper">
      <div class="new-products swiper-wrapper">
        {%- for block in section.blocks -%}
            <div class="gpro__item swiper-slide">
                <a href="{{ block.settings.link }}" class="pro__link default_item" aria-label="{{ block.settings.image.alt }}">
                   <div class="pro_name">{{ block.settings.name }}</div>
                  <img src="{{ block.settings.image | img_url:"master" }}" />
                </a>

            </div>
        {%- endfor -%}
      </div>
    </div>
  {%- endif -%}
 
</div>
</div></div>
</div>

<div class="mySlides fade">
  <div class="numbertext">2 / 3</div>
    <div class="row">
  <div class="col-md-3">
    <h2>Column 1</h2>
    <textarea name="column_1_text">{{ section.settings.column_1_text }}</textarea>
  </div>
  <div class="col-md-3">
    <h2>Column 2</h2>
    <textarea name="column_2_text">{{ section.settings.column_2_text }}</textarea>
  </div>
  <div class="col-md-3">
    <h2>Column 3</h2>
    <textarea name="column_3_text">{{ section.settings.column_3_text }}</textarea>
  </div>
  <div class="col-md-3">
    <h2>Column 4</h2>
    <textarea name="column_4_text">{{ section.settings.column_4_text }}</textarea>
  </div>
</div>
  <div class="text">Caption Two</div>
</div>

<div class="mySlides fade">
  <div class="numbertext">3 / 3</div>
  <div class="row">
  <div class="col-md-3">
    <h2>Column 1</h2>
    <textarea name="column_1_text">{{ section.settings.column_1_text }}</textarea>
  </div>
   </div> 
  <div class="text">Caption Three</div>
</div>

<a class="prev" onclick="plusSlides(-1)">❮</a>
<a class="next" onclick="plusSlides(1)">❯</a>
</div>
<br>
<div style="text-align:center">
  <span class="dot" onclick="currentSlide(1)"></span> 
  <span class="dot" onclick="currentSlide(2)"></span> 
  <span class="dot" onclick="currentSlide(3)"></span> 
</div>
</body>
</html> 

   {%- if section.settings.divider -%}</div>{%- endif -%}
   <script>
let slideIndex = 1;
showSlides(slideIndex);

function plusSlides(n) {
  showSlides(slideIndex += n);
}

function currentSlide(n) {
  showSlides(slideIndex = n);
}

function showSlides(n) {
  let i;
  let slides = document.getElementsByClassName("mySlides");
  let dots = document.getElementsByClassName("dot");
  if (n > slides.length) {slideIndex = 1}    
  if (n < 1) {slideIndex = slides.length}
  for (i = 0; i < slides.length; i++) {
    slides[i].style.display = "none";  
  }
  for (i = 0; i < dots.length; i++) {
    dots[i].className = dots[i].className.replace(" active", "");
  }
  slides[slideIndex-1].style.display = "block";  
  dots[slideIndex-1].className += " active";
}
</script>

{% schema %}
{
  "name": "multi product",
  "class": "index-section",
  "max_blocks": 4,
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "t:sections.logo-list.settings.title.label"
    
    },
    {
      "type": "text",
      "id": "column_1_text",
      "label": "t:sections.logo-list.settings.title.label"
    
    },
    {
      "type": "text",
      "id": "column_2_text",
      "label": "t:sections.logo-list.settings.title.label"
    
    },
    {
      "type": "text",
      "id": "column_3_text",
      "label": "t:sections.logo-list.settings.title.label"
    
    },
    {
      "type": "text",
      "id": "column_4_text",
      "label": "t:sections.logo-list.settings.title.label"
    
    },
    {
      "type": "select",
      "id": "heading_size",
      "label": "t:common.heading_size.label",
      "default": "h2",
      "options": [
        {
          "value": "h3",
          "label": "t:common.heading_size.options.small.label"
        },
        {
          "value": "h2",
          "label": "t:common.heading_size.options.medium.label"
        },
        {
          "value": "h1",
          "label": "t:common.heading_size.options.large.label"
        },
        {
          "value": "h0",
          "label": "t:common.heading_size.options.extra_large.label"
        }
      ]
    },
    {
      "type": "select",
      "id": "heading_position",
      "label": "t:common.heading_position.label",
      "default": "left",
      "options": [
        {
          "value": "left",
          "label": "t:common.heading_position.options.left.label"
        },
        {
          "value": "center",
          "label": "t:common.heading_position.options.center.label"
        },
        {
          "value": "right",
          "label": "t:common.heading_position.options.right.label"
        }
      ]
    },
    {
      "type": "text",
      "id": "button_label1",
      "label": "t:sections.text-and-image.settings.button_label.label"
    },
    {
      "type": "url",
      "id": "button_link1",
      "label": "t:sections.text-and-image.settings.button_link.label"
    },
    {
      "type": "text",
      "id": "button_label2",
      "label": "t:sections.text-and-image.settings.button_label.label"
    },
    {
      "type": "url",
      "id": "button_link2",
      "label": "t:sections.text-and-image.settings.button_link.label"
    },
    {
      "type": "checkbox",
      "id": "divider",
      "label": "t:sections.logo-list.settings.divider.label",
      "default": false
    }
  ],
  "blocks": [
    {
      "type": "item",
      "name": "item",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "image"
        },
        {
          "type": "text",
          "id": "name",
          "label": "product title"
        },
        {
          "type": "url",
          "id": "link",
          "label": "link"
        }
        
      ]
    }
  ],
  "presets": [
    {
        "name": "multi product"
     
    }
  ]
 
}
{% endschema %}
<script>
  $('.slider').slick({
  dots: true, // show navigation dots
  arrows: true, // show navigation arrows
  infinite: false, // disable infinite loop
  speed: 300, // animation speed
  slidesToShow: 1, // number of visible slides at a time
  adaptiveHeight: true // adjust height of slides dynamically
});
</script>

